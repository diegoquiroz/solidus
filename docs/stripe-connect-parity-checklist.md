# Stripe Connect Parity Checklist (M4)

Status: signed off

- [x] Create connected account helper (`api.connect.createAccount`)
- [x] Retrieve connected account helper (`api.connect.retrieveAccount`)
- [x] Create onboarding account link helper (`api.connect.createAccountLink`)
- [x] Create dashboard login link helper (`api.connect.createLoginLink`)
- [x] Create transfer helper (`api.connect.createTransfer`)
- [x] Keep account status sync path wired through `account.updated` webhook handler
- [x] Add test coverage for account workflows and transfer helper
- [x] Document onboarding and payout readiness caveats

Signed off: 2026-02-19
